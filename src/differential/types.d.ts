export type Fact = true | false | 'unknown';
export interface AssessmentEvidence {id:string;domain:'mood'|'anxiety'|'ocd'|'screen'|'unsupported';concept:string;value:Fact;sourceModule:'mood'|'anxiety'|'ocd'|'router'|'clarification';sourceType:'standardized_screener'|'structured_assessment'|'free_text_extraction'|'functional_impairment'|'duration'|'exclusion'|'user_clarification';temporality:'current'|'history'|'unknown';certainty:'reported'|'unknown';relevance:'context'|'discriminating';provenance:{questionIds:string[];revision:number;engineVersion:string};}
export interface Reason {key:string;concepts:string[];evidenceIds:string[];}
export interface CandidateDirection {id:string;domain:string;labelKey:string;consistency:'insufficient'|'weak'|'supported'|'strong';level:0|1|2|3;supporting:Reason[];opposing:Reason[];discriminatingFeatures:Reason[];missingEvidence:string[];unresolvedRuleOuts:string[];}
export interface DifferentialResult {status:'sufficient'|'insufficient'|'conflicted';primaryDirections:string[];secondaryDirections:string[];possibleCooccurringPatterns:{directions:string[];key:string}[];alternatives:string[];unsupportedDomainFlags:string[];unresolvedRuleOuts:string[];contradictions:{concept:string;evidenceIds:string[];selectedEvidenceIds:string[];selectedValue:Fact;policy:'higher_quality_preferred_requires_review'|'unresolved'}[];missingEvidence:string[];safetyState:{urgency:'none'|'attention'|'elevated'|'acute';assessmentStatus:string;requiresInterruption:boolean;primaryDomain:string|null;intrusiveInterpretation:{harm:boolean;self:boolean;notAnOCDDiagnosis:boolean}};reasoning:{supporting:Reason[];opposing:Reason[];discriminatingFeatures:Reason[]};engineVersion:'cross-disorder-differential-v1.0.0';candidates:CandidateDirection[];matchedRules:string[];evidence:AssessmentEvidence[];provenance:Record<string,string[]>;blocked:boolean;availability?:Record<string,boolean>;}
