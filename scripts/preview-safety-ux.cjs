// Local test fixture server only. Not part of dist or the delivered application routes.
const fs=require('fs'),path=require('path'),http=require('http'),root=path.resolve(__dirname,'../dist');
const scripts=[...fs.readFileSync(path.join(root,'index.html'),'utf8').matchAll(/<script src="([^"]+)"/g)].map(m=>m[1]).filter(p=>p!=='app.js').map(p=>`<script src="/assets/${p}"></script>`).join('');
http.createServer((req,res)=>{const url=new URL(req.url,'http://localhost');if(url.pathname==='/'){res.setHeader('Content-Type','text/html;charset=utf-8');res.end(fs.readFileSync(path.resolve(__dirname,'../tests/safety-ux-preview.html'),'utf8').replace('<!-- SHARED SCRIPTS -->',scripts));return;}const file=path.resolve(root,'.'+url.pathname.replace(/^\/assets/,''));if(!url.pathname.startsWith('/assets/')||!file.startsWith(root+path.sep)){res.writeHead(404);res.end();return;}fs.readFile(file,(err,b)=>{if(err){res.writeHead(404);res.end();return;}res.setHeader('Content-Type',file.endsWith('.js')?'text/javascript;charset=utf-8':file.endsWith('.css')?'text/css;charset=utf-8':'text/plain;charset=utf-8');res.setHeader('Cache-Control','no-store');res.end(b);});}).listen(4174,'127.0.0.1',()=>console.log('Synthetic UX fixtures: http://127.0.0.1:4174/'));
