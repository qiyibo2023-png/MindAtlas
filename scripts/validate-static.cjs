const fs=require('fs'),path=require('path'),vm=require('vm');const root=path.resolve('dist');const html=fs.readFileSync(path.join(root,'index.html'),'utf8');const assets=[...html.matchAll(/(?:src|href)="([^"]+)"/g)].map(m=>m[1]).filter(s=>!s.startsWith('data:')&&!s.startsWith('http'));for(const asset of assets){if(!fs.existsSync(path.join(root,asset)))throw Error('Missing '+asset);if(asset.endsWith('.js'))new vm.Script(fs.readFileSync(path.join(root,asset),'utf8'),{filename:asset});}if(/fonts.googleapis/.test(fs.readFileSync('dist/style.css','utf8')))throw Error('External font');console.log('PASS static assembly: '+assets.length+' local assets exist and JavaScript parses. No compilation step is required for this static project.');
